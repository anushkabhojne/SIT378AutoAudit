apiVersion: v1
kind: Secret

metadata:
  name: plugin-manager-secrets

type: Opaque

data:
  #Base64 encoded secrets
  ENCRYPTION_KEY: <base64-encoded-encryption-key>
  CREDENTIAL_STORE_PATH: <base64-encoded-credential-store-path>
